//= require fullcalendar/gcal
var google_cal_id = "<%= ENV.fetch("GOOGLE_CAL_ID") %>";
var google_cal_api_key = "<%= ENV.fetch("GOOGLE_CAL_API_KEY") %>";
$(document).ready(function() {
    $('#calendar').fullCalendar({
        googleCalendarApiKey: google_cal_api_key,
        header: {
								    left:   'prev,next today',
								    center: 'title',
								    right:  ''
								},
        eventSources: [
            {
                googleCalendarId: google_cal_id
            },
            {
                googleCalendarId: google_cal_id,
                className: 'nice-event'
            }
        ],
        editable: true,
        eventLimit: true,
    eventMouseover: function(calEvent, jsEvent, view) {

        alert('Event: ' + calEvent.title);
        alert('Coordinates: ' + jsEvent.pageX + ',' + jsEvent.pageY);
        alert('View: ' + view.name);

        // change the border color just for fun
        $(this).css('border-color', 'red');

    }
  });
});

    //     eventSources: [
    //     {
		  //   events: [
		  //       {
		  //           title  : '7:30am-8:00am',
		  //           start  : '2017-07-05'
		  //       },
		  //       {
		  //           title  : 'event2',
		  //           start  : '2017-07-06',
		  //           end    : '2017-07-08'
		  //       },
		  //       {
		  //           title  : 'event3',
		  //           start  : '2017-07-09T12:30:00',
		  //           allDay : false // will make the time show
		  //       }
		  //   ],
    //         color: 'yellow',     // an option!
    //         textColor: 'black', // an option!
    //         editable: true,
    //         navLinks: true
    //       }
    // ],
    // eventClick: function(calEvent, title) {

    //     alert('Event: ' + calEvent.title + ' ' + 'Select this appointment?');

    //     // change the border color just for fun
    //     $(this).css('border-color', 'red');

    // }